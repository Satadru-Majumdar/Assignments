n = int(input())
if n <= 1:
    print("Not Prime")
else:
    is_prime = 1
    for i in range(2, n):
        if n % i == 0:
            is_prime = 0
            break
    if is_prime == 1:
        print("Prime")
    else:
        print("Not Prime")